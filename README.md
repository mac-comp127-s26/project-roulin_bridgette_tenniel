# President Trump's Adventure

The project is now set up with a simple story file based on `Map`.

## Current files

- `src/story/StoryData.java`: returns an empty story map for your group to fill in
- `src/Main.java`: loads the story map

## Suggested next step

Keep story content in the map for now, then later connect it to
`macalester.graphics` for:

- background image
- dialogue box
- next button
- choice buttons
- top-right stats
